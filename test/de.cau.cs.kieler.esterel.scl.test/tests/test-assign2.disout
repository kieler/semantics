module test_assign2:
output A : boolean;
output B : boolean;

  pause;
  pause;
  emit A(false);
  pause;
  emit A(true)
||
  var value : boolean in
    value := true;
    loop
      abort
        loop
          pause
        end loop
      when A;
      value := ?A;
      emit B(value)
    end loop
  end var

end module
