scchart illegalstrongabort {
  input signal pure I;
  output signal pure O;
  bool L = false;

  initial state C {

    initial state _
    --> _ with I / L = true;
  }
  o-> D with L / O;

  state D;
}   