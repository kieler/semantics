scchart S0 "non_reactive" {
  output bool A;

  state _S3
  --> _Depth2 immediate with / A = A || true;

  initial state S0 "s1"
  --> _Depth;

  state _Depth
  --> _Depth2 immediate with A
  --> _S3 immediate;

  state _Depth2
  --> _Depth2;

  region _During:

  initial state _I
  --> _S immediate with / A = false;

  state _S
  --> _I;
}