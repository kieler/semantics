scchart _51_initial_immediate_with_hierarchy_and_concurrency2
"51-initial-immediate-with-hierarchy-and-concurrency" {
  output bool O;
  output bool L;

  initial state A {

    initial state B
    --> _Depth immediate with / L = L || true;

    state _Depth
    --> _Depth;
  };

  region:

  initial state _Depth2
  --> _S immediate with L
  --> _Pause2 immediate;

  state _S
  --> _Depth immediate with / O = O || true;

  state _Depth
  --> _Depth;

  state _Pause2
  --> _Depth2;

  region _During:

  state _S
  --> _Depth;

  initial state _Depth
  --> _S3 immediate with / O = false;

  state _S3
  --> _S immediate with / L = false;
}