scchart _108_hiearchy_final "108-hiearchy-final" {
  output bool O;

  initial state A {

    initial state B2 "B"
    --> _S;

    final state F;

    state _S
    --> F immediate with / O = O || true;
  }
  >-> _HaltState;

  state _HaltState
  --> _HaltState;

  region _During:

  initial state _I
  --> _S immediate with / O = false;

  state _S
  --> _I;
}