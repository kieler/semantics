scchart _82_preemption_strong_prio_after_wakeup
"82-preemption-strong-prio-after-wakeup" {
  input bool I1;
  input bool I2;
  output bool O2;
  output bool O1;

  initial state A
  --> G;

  state G {

    final state _Aborted;

    state _S
    --> D immediate with / O2 = O2 || true;

    initial state E
    --> _Depth;

    state _Depth
    --> _Aborted immediate with I1
    --> _S3 immediate;

    state _S3
    --> _S immediate with I2
    --> E immediate;

    state D
    --> _Depth2;

    state _Depth2
    --> _Aborted immediate with I1
    --> D immediate;
  }
  >-> _S immediate;

  state _S
  --> A immediate with / O1 = O1 || true;

  region _During:

  state _S
  --> _Depth;

  initial state _Depth
  --> _S3 immediate with / O2 = false;

  state _S3
  --> _S immediate with / O1 = false;
}