scchart _155_surfacedepth "155-surfacedepth" {
  input bool I1;
  input bool I2;
  output bool O1;
  output bool O2;
  input bool I3;
  input bool I4;
  output bool O3;
  output bool O4;

  state A
  --> _S immediate with I1
  --> _S5 immediate;

  initial state I
  --> A;

  state _S
  --> _Depth2 immediate with / O1 = O1 || true;

  state _S2
  --> _Depth3 immediate with / O2 = O2 || true;

  state _S3
  --> _Depth4 immediate with / O3 = O3 || true;

  state _S4
  --> _Depth5 immediate with / O4 = O4 || true;

  state _S5
  --> _S3 immediate with I3
  --> _Pause immediate;

  state _Pause
  --> _Depth;

  state _Depth
  --> _S immediate with I1
  --> _S7 immediate;

  state _S7
  --> _S2 immediate with I2
  --> _S8 immediate;

  state _S8
  --> _S3 immediate with I3
  --> _S9 immediate;

  state _S9
  --> _S4 immediate with I4
  --> _Pause immediate;

  state _Depth2
  --> _Depth2;

  state _Depth3
  --> _Depth3;

  state _Depth4
  --> _Depth4;

  state _Depth5
  --> _Depth5;

  region _During:

  state _S
  --> _Depth;

  initial state _Depth
  --> _S3 immediate with / O1 = false;

  state _S3
  --> _S4 immediate with / O2 = false;

  state _S4
  --> _S5 immediate with / O3 = false;

  state _S5
  --> _S immediate with / O4 = false;
}