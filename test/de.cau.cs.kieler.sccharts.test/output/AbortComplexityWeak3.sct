
@diagram[KLayLayered] false
@layout[direction] DOWN
scchart AbortComplexityWeak {
  input bool R;
  output int O;
  output int O2;
  output bool A;
  bool _trig;

  region Main " ":

  initial state S {

    region _Entry:

    state _Main {

      initial state I
      --> _Aborted immediate with _trig
      --> _Pause immediate;

      state _Depth2
      --> I immediate with / O2 ++;

      final state _Aborted;

      state _Depth
      --> _Depth2 immediate with / O ++;

      state _Pause
      --> _Depth;

      region _Ctrl:

      final state _Done;

      state _S
      --> _Done immediate with / _trig = true;

      initial state _Run
      --> _Depth;

      state _Depth
      --> _S immediate with R
      --> _Run immediate;
    }
    >-> _Done;

    initial state _Init
    --> _Main immediate with / _trig = false;

    final state _Done;
  }
  >-> _S immediate;

  state _S
  --> _S2 immediate with / A = true;

  state _S2
  --> _Depth immediate with / O2 ++;

  state _Depth
  --> _Depth;
}