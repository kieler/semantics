
@Transformation DURING
scchart DuringAction {
  input bool I;
  output bool O;

  initial state A {

    initial state A0
    --> A1;

    state A1
    --> A0;

    region _During:

    initial state _I
    --> _I with I / O = !O;
  };
}