@Transformation DURING
scchart DuringAction {
	input bool I;
	output bool O;

    initial state A{ 
      during I / O = !O;
      
      initial state A0
      --> A1;
      
      state A1
      --> A0;
    };
}