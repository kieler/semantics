
@Transformation SIGNAL
scchart ValuedSignal {
  bool x;
  int x_val;
  int x_curval;
  immediate during / x = false;
  immediate during x / x_val = x_curval;
  immediate during / x_curval = 0;

  initial state A
  --> B with / x_curval = 3; x = x || true;

  state B;
}