scchart ExitTest {
  input bool I;
  output bool O;

  initial state A {

    state _Done2
    --> _S immediate with I
    --> _S5 immediate;

    final state _S5;

    state _S
    --> _S5 immediate / O = false;

    initial state _Run
    --> _Depth;

    state _Depth
    --> _Done2 immediate with true & I
    --> _Run immediate;
  }
  >-> F immediate;

  final state F;
}