scchart EntryTest {
  output bool O;

  initial state A {

    region _Entry:

    initial state _Init
    --> _Done immediate / O = false;

    final state _Done;
  }
  >-> _Exit;

  final state _Exit;
}