scchart EntryTest {
  input bool I;
  output bool O;

  initial state A {

    region _Entry:

    initial state _Init
    --> _S immediate with I
    --> _S4 immediate;

    final state _S4;

    state _S
    --> _S4 immediate / O = false;
  }
  >-> _Exit;

  final state F;

  state _Exit
  >-> F with !I;
}