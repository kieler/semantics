scchart ExitTest {
  input bool I;
  output bool O;

  initial state A {

    region _Exit:

    initial state _Init
    --> _C immediate / O = false;

    final state _S4;

    state _C
    --> _C2 immediate / O = true;

    state _C2
    --> _S immediate with I
    --> _S4 immediate;

    state _S
    --> _S4 immediate / O = !O;
  }
  >-> F;

  final state F;
}