
@Transformation DEFERRED
scchart DeferredTransition {
  input int x;
  bool _deferred = false;

  initial state A
  --> B with x == 1 / _deferred = true
  --> B with x == 2;

  state B {
    during / _deferred = false;

    initial state B0
    --> B2
    --> B1 immediate with !_deferred & x == 3;

    state B1
    --> B2 immediate with !_deferred & x == 4;

    state B2;
  }
  --> C immediate with !_deferred & x == 5;

  state C
  --> B;
}