
@Transformation INITIALIZATION
scchart InitializationTest {

  initial state A {
    int x;
    bool y;
    float z;
    entry / x = 5;
    entry / y = true;
    entry / z = 1.1;

    initial state A0;
  };
}