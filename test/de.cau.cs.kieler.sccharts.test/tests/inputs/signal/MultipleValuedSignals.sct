@Transformation SIGNAL
scchart MultipleValuedSignals {
  signal int x = 1;
  signal int y = 2;
  
  region R1:
	  initial state A
	  --> B with / x(3);
	  
	  final state B;
	  
  region R2:
  	initial state C
  	--> D with x / y(val(x));
  	 
  	final state D;
}