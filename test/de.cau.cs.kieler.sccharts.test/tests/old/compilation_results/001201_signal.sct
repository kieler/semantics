scchart ^signal {
  input bool I;
  output bool O;

  initial state A {

    state _Depth
    --> A2 immediate / O = O | true;

    initial state _Depth2
    --> _Depth;

    state A2
    --> _Depth2;
  };

  region _During:

  initial state _Depth
  --> _S immediate / O = false;

  state _S
  --> _Depth;
}