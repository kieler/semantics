scchart abort {
  input bool I;

  initial state A {

    final state _Aborted;

    initial state _S2
    --> _Depth;

    state _Depth
    --> _Aborted immediate with I
    --> _S2 immediate;
  }
  >-> B immediate;

  final state B;
}