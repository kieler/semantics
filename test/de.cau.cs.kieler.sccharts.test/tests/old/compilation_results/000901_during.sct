scchart ^during {
  output bool O;

  initial state A {

    final state _Depth;

    initial state A1
    --> _Depth;

    region _During:

    state _Depth
    --> _I immediate / O = false;

    initial state _I
    --> _Depth;
  };
}