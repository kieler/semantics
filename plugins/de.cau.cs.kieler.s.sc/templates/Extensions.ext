import s;
import kexpressions;
import utilities;


List[Signal] allSignals(s::Program program) :
	program.eAllContents.select(e|e.metaType == Signal)
;

int getHighestPriority(Program program) :
	5
;
