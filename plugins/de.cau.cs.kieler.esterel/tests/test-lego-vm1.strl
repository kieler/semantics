module testlego1 :
      input TOUCH_1;
      output CPUTS(integer), MOTOR_A_DIR(integer), MOTOR_A_SPEED(integer);

loop
     emit CPUTS(1);
     emit MOTOR_A_DIR(1);
     emit MOTOR_A_SPEED(20);
     pause;
end loop.

